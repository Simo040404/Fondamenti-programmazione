#ifndef _OPERATION_H_
#define _OPERATION_H_

// macro con le cinque operazioni
#define MY_SUM(x,y) ((x) + (y))
#define MY_SUB(x,y) ((x) - (y))
#define MY_MUL(x,y) ((x) * (y))
#define MY_DIV(x,y) ((x) / (y))
#define MY_MOD(x,y) ((x) % (y))

// macro per il nome e il cognome
#define NAME "Stefano"
#define SURNAME "Marchese"
#define PRINT_NAME_SURNAME printf("%s %s\n\n", NAME, SURNAME);

#endif // _OPERATION_H_